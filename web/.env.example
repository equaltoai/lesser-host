# Dev-only proxy targets (Vite). These do not affect production routing.
LESSER_HOST_CONTROL_PLANE_ORIGIN=http://localhost:8787
LESSER_HOST_TRUST_ORIGIN=http://localhost:8787

# Wallet chain id for signature challenges (defaults to 1 if unset).
VITE_WALLET_CHAIN_ID=1

