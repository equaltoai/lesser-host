package hostedgenesis

// Endpoint-turn bridge tests moved into dispatch_test.go, where the Host
// dispatcher now proves POST /microvms/{session_id}/invoke/hosted-genesis/turn
// through AppTheory's canonical controller route.
