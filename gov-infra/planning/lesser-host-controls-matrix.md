# lesser-host Controls Matrix (custom — v0.1.3)

This matrix is the “requirements → controls → verifiers → evidence” backbone for lesser-host. It is intentionally
engineering-focused: it does not claim compliance, but it makes security/quality assertions traceable and repeatable.

## Scope
- **System:** AWS-backed multi-service control plane for hosted Lesser instance provisioning + governance + trust/safety services (Go Lambdas, Svelte web UI, AWS CDK infra, Solidity contracts).
- **In-scope data:** authentication/session tokens, wallet addresses, instance API keys (hashed), Stripe billing identifiers, x402 invocation grant hashes/usage slots, AI provider prompts/outputs (may contain sensitive user content), bounded soul comm mailbox content/state, operational telemetry, deploy receipts/artifacts, secrets in AWS SSM/KMS (no secrets in git).
- **Environments:** `lab` (default), `staging`, `prod`; “prod-like” means *internet-reachable* and/or *connected to real third-party services* (AWS accounts, Stripe, AI providers) even if “test” data.
- **Third parties:** AWS (Lambda, DynamoDB, S3, SQS, KMS, Route53, CloudFront, CodeBuild, Organizations), Stripe, caller-provided x402 facilitators, Anthropic, OpenAI, Ethereum JSON-RPC providers, GitHub Actions.
- **Out of scope:** tenant application internals (the per-tenant `lesser` deployments) except where the control plane ingests deploy receipts or calls tenant endpoints; end-user device security.
- **Assurance target:** audit-ready hardening for security-critical paths (authn/authz, secrets, supply chain, public-facing HTTP endpoints), with deterministic, CI-enforced verifiers.

## Threats (reference IDs)
- Threats are stable IDs (`THR-*`) defined in `gov-infra/planning/lesser-host-threat-model.md`.
- Each `THR-*` must map to ≥1 row in the controls table below (validated by a deterministic parity check).

## Status (evidence-driven)
If you track implementation status, treat it as evidence-driven:
- `unknown`: no verifier/evidence yet
- `partial`: some controls exist but coverage/evidence is incomplete
- `implemented`: verifier exists and evidence path is repeatable

## Engineering Controls (Threat → Control → Verifier → Evidence)
This table is the canonical mapping used by the rubric/roadmap/evidence plan.

| Area | Threat IDs | Control ID | Requirement | Control (what we implement) | Verification (command/gate) | Evidence (artifact/location) |
| --- | --- | --- | --- | --- | --- | --- |
| Quality | THR-1 THR-8 THR-9 | QUA-1 | Unit tests prevent regressions | Run Go + Web unit tests as first-line regression gates. | `bash gov-infra/verifiers/gov-verify-rubric.sh` (QUA-1) | `gov-infra/evidence/QUA-1-output.log` |
| Quality | THR-2 THR-4 THR-6 | QUA-2 | Integration/contract tests prevent boundary drift | Keep infra/contract surface reproducible (contracts compile, CDK synth). | `bash gov-infra/verifiers/gov-verify-rubric.sh` (QUA-2) | `gov-infra/evidence/QUA-2-output.log` |
| Quality | THR-1 THR-4 THR-9 | QUA-3 | Coverage threshold is enforced (no dilution) | Measure and enforce Go coverage ≥ 80% as a floor; add JS coverage once configured. | `bash gov-infra/verifiers/gov-verify-rubric.sh` (QUA-3) | `gov-infra/evidence/QUA-3-output.log` + `gov-infra/evidence/coverage.out` |
| Consistency | — | CON-1 | Formatting is clean (no diffs) | Enforce `gofmt` cleanliness for all Go sources in-repo. | `bash gov-infra/verifiers/gov-verify-rubric.sh` (CON-1) | `gov-infra/evidence/CON-1-output.log` |
| Consistency | THR-1 THR-5 THR-7 THR-10 | CON-2 | Lint/static analysis is enforced (pinned toolchain) | Enforce Go lint (golangci-lint + gosec), Solidity lint (solhint), and TS lint/typecheck in web/cdk with pinned toolchain versions (no “whatever is installed”). | `bash gov-infra/verifiers/gov-verify-rubric.sh` (CON-2) | `gov-infra/evidence/CON-2-output.log` |
| Consistency | THR-2 THR-4 | CON-3 | Public boundary contract parity (if applicable) | Define and enforce contract parity for public boundaries (e.g., deploy receipt schema, attestations schema, and agent-first REST/SSE API invariants). | `bash gov-infra/verifiers/gov-verify-rubric.sh` (CON-3) | `gov-infra/evidence/CON-3-output.log` |
| Completeness | THR-1 THR-5 | COM-1 | All modules compile (no “mystery meat”) | Ensure all Go modules compile from a clean checkout (root + nested modules like `cdk/`). | `bash gov-infra/verifiers/gov-verify-rubric.sh` (COM-1) | `gov-infra/evidence/COM-1-output.log` |
| Completeness | THR-5 THR-10 | COM-2 | Toolchain pins align to repo expectations | Ensure Go and Node versions match the repo’s declared constraints (go.mod, CI, package engines); ensure security tools are pinned before they are trusted. | `bash gov-infra/verifiers/gov-verify-rubric.sh` (COM-2) | `gov-infra/evidence/COM-2-output.log` |
| Completeness | THR-5 | COM-3 | Lint config schema-valid (no silent skip) | Validate that lint configs are parseable and supported by the pinned toolchain (avoid “silent ignore”). | `bash gov-infra/verifiers/gov-verify-rubric.sh` (COM-3) | `gov-infra/evidence/COM-3-output.log` |
| Completeness | THR-1 THR-9 | COM-4 | Coverage threshold not diluted | Ensure the configured threshold is ≥ 80% and consistent across rubric/docs/verifiers. | `bash gov-infra/verifiers/gov-verify-rubric.sh` (COM-4) | `gov-infra/evidence/COM-4-output.log` |
| Completeness | THR-5 THR-7 | COM-5 | Security scan config not diluted | Disallow excluding high-signal security rules without explicit, version-bumped policy decisions. | `bash gov-infra/verifiers/gov-verify-rubric.sh` (COM-5) | `gov-infra/evidence/COM-5-output.log` |
| Security | THR-5 THR-7 | SEC-1 | Baseline SAST stays green | Run dedicated security/static scan gates (gosec via golangci-lint + Slither for Solidity). | `bash gov-infra/verifiers/gov-verify-rubric.sh` (SEC-1) | `gov-infra/evidence/SEC-1-output.log` |
| Security | THR-5 | SEC-2 | Dependency vulnerability scan stays green | Run dependency vulnerability scanning with pinned tooling (Go first; add Node tooling once chosen). | `bash gov-infra/verifiers/gov-verify-rubric.sh` (SEC-2) | `gov-infra/evidence/SEC-2-output.log` |
| Security | THR-5 THR-10 | SEC-3 | Supply-chain verification green | Pin GitHub Actions by commit SHA; scan Node dependency lifecycle hooks (scripts disabled install); flag risky go.mod replace directives and Python custom indexes. | `bash gov-infra/verifiers/gov-verify-rubric.sh` (SEC-3) | `gov-infra/evidence/SEC-3-output.log` |
| Security | THR-3 THR-4 THR-6 | SEC-4 | Domain-specific P0 regression tests | Add explicit P0 safety/security regression gates (authz invariants, SSRF defense, prompt boundaries, no sensitive logs). | `bash gov-infra/verifiers/gov-verify-rubric.sh` (SEC-4) | `gov-infra/evidence/SEC-4-output.log` |
| Security | THR-12 | SEC-4 | x402 grant authority stays scoped/minimized | Keep x402 invocation grants off-chain and scoped to `agentId`/capability/tool/resource/request-hash; store only hashes for raw caller/payment evidence and grant tokens; mark facilitator trust as caller-provided/unverified; require instance-key/domain ownership to consume; enforce expiry/max-usage/idempotency. | `bash gov-infra/verifiers/gov-verify-rubric.sh` (SEC-4) + `go test ./internal/controlplane -run 'TestP0_HandleSoulX402IssueInvocationGrant'` | `gov-infra/evidence/SEC-4-output.log` |
| Consistency | THR-12 THR-2 | CON-3 | x402 grant contract parity | Keep the REST schemas/generated adapter for grant issue/consume synchronized so lesser-body consumes a stable scoped-grant contract. | `bash gov-infra/verifiers/gov-verify-rubric.sh` (CON-3) + `cd web && npm run verify:lesser-host-contracts` | `gov-infra/evidence/CON-3-output.log` |
| Compliance | THR-11 THR-10 | CMP-4 | Bounded mailbox authority controls stay explicit | Enforce the host-authoritative mailbox boundary: retention, encryption, access audit, list/content split, no semantic-memory role, hash-only instance auth, write-once events, protected identity/provenance fields, and no body-owned mailbox truth. | `bash gov-infra/verifiers/gov-verify-rubric.sh` (CMP-4) | `gov-infra/evidence/CMP-4-output.log` |
| Docs | THR-1 THR-10 | DOC-5 | Threat model ↔ controls parity (no unmapped threats) | Enforce “named threats must map to controls” via deterministic parity check. | `bash gov-infra/verifiers/gov-verify-rubric.sh` (DOC-5) | `gov-infra/evidence/DOC-5-parity.log` |
| Security | T-CSP-001 | C-CSP-FT | CSP byte-string freeze | Lock the CloudFront CSP header as an exact byte-string in `cdk/lib/lesser-host-stack.ts`. The SEC-5 verifier extracts the CSP arrays, joins with `; ` (per CDK), and compares byte-for-byte against locked-in expected values. Any drift — changed directive, reordered directive, changed value, changed join separator, changed count — fails CI. | `bash gov-infra/verifiers/sec/web-csp-integrity.sh` (SEC-5) | `gov-infra/evidence/SEC-5-output.log` |
| Security | T-OAC-001 | C-OAC-MUT | OAC form transport mutation lock | Enforce OAC form transport integrity in the built web HTML. The SEC-7 verifier checks that the `startAwsOacFormTransport` function is present and its transport signals (`data-facetheory-oac-form`, `x-amz-content-sha256`) are intact. | `bash gov-infra/verifiers/sec/oac-form-integrity.sh` (SEC-7) | `gov-infra/evidence/SEC-7-output.log` |
| Security | T-COMP-001 | C-CDN-COMP | CDN CloudFront composition lock | Enforce CloudFront distribution composition: required behaviors present (API, trust, SSR hydration), SPA rewrite function present, response headers policy applied with correct security headers, OAC origins configured for S3 buckets, legacy operator/UI mint-conversation streaming routes pinned to the SSE-capable origin, and the exact Lesser-used instance-key registration mint-conversation route pinned to the JSON control-plane origin so `HostedGenesisSession` remains user-visible authority. SEC-6 enforces no inline HTML; SEC-8 enforces distribution shape. | `bash gov-infra/verifiers/sec/cloudfront-composition.sh` (SEC-8); `cd cdk && npm test`; `bash gov-infra/verifiers/sec/web-html-inline-absence.sh` (SEC-6) | `gov-infra/evidence/SEC-8-output.log`; `gov-infra/evidence/SEC-6-output.log` |
| Security | T-CCIP-UA-001 | C-CCIP-UA | ENS CCIP-read no-User-Agent compatibility lock | Preserve CloudFront WAF protection while allowing common ENS CCIP-Read clients that omit `User-Agent`: count AWS `AWSManagedRulesCommonRuleSet` `NoUserAgent_HEADER`, then block that managed label on every path except exact `/resolve`. This keeps non-`/resolve` no-`User-Agent` traffic edge-blocked and leaves sender validation, signing, known-bad-input rules, and rate limits intact. | `bash gov-infra/verifiers/sec/cloudfront-composition.sh` (SEC-8); `cd cdk && npm test` | `gov-infra/evidence/SEC-8-output.log` |
| Security | T-AUTH-DRIFT-001 | C-AUTH-LOCK | Trust-auth / attestation signing change-lock | Enforce preservation of trust-api instance authentication (sha256 key hash, never raw keys) and attestation signing invariants. SEC-9 change-lock verifier diffs current code against a known-good base ref; any change to auth flow, key storage, hash comparison, or attestation signing must be intentional and reviewed. | `bash gov-infra/verifiers/sec/trust-auth-preservation.sh` (SEC-9) | `gov-infra/evidence/SEC-9-output.log` |
| Security | T-SUPPLY-001 | C-SUPPLY-LOCK | Supply-chain release verification change-lock | Enforce preservation of Consumer release verification for lesser/body artifacts. SEC-10 change-lock verifier diffs current code against a known-good base ref; any change to exact-version compatibility admission, checksum verification, release manifest handling, artifact download, template/auxiliary-asset verification, or provisioning deploy path must be intentional and reviewed. Project 17 M15 (`#960` / `#964` / PR #965) raises the exact managed Body floor to v1.0.8 and aligns fail-closed three-phase fixtures. The only acceptable non-empty diff state is the exact full reviewed governance-event fingerprint embedded in the verifier; any additional locked-file semantic drift changes the fingerprint and fails closed. | `bash gov-infra/verifiers/sec/release-verification-preservation.sh` (SEC-10) | `gov-infra/evidence/SEC-10-output.log` |
| Consistency | T-MCP-ROUTE-001 | C-MCP-ROUTE | Wire-MCP route ownership | Enforce host-owned MCP route ownership in the build pipeline. CON-4 verifier checks that `build.sh` has a `RUN_MODE=lesser-mcp` branch, the INLINE marker for `build-lesser-mcp.sh` is present, no host-owned routes are duplicated or misattributed, and the rendered buildspec contains the inlined MCP URL template. | `bash gov-infra/verifiers/con/wire-mcp-route-ownership.sh` (CON-4) | `gov-infra/evidence/CON-4-output.log` |
| Security | T-TENANT-LEAK-001 | C-TENANT-SCOPE | Portal stack-state reads enforce tenant ownership first | Source-level assertion that the `requireInstanceAccess` ownership check call precedes all stack-state database read calls (`categorizeLatestUpdateJobs`, `loadProvisionJobFallback`) within the `handlePortalGetInstanceStack` handler body. The SEC-11 verifier extracts line numbers and enforces strict `ownership-line < read-line` ordering. | `bash gov-infra/verifiers/sec/portal-stack-state-tenant-scoping.sh` (SEC-11) | `gov-infra/evidence/SEC-11-output.log` |
| Security | T-OPER-AUTH-001 T-DRIFT-CROSS-TENANT-001 | C-OPER-AUTH | Operator fleet endpoints protected by auth + operator-role gating | Source-level assertion that all three operator fleet endpoints (`/api/v1/operators/releases`, `/api/v1/operators/instances/drift`, `/api/v1/operators/instances/remediate-mcp-drift`) are gated by both `apptheory.RequireAuth()` on the route and `requireOperator(ctx)` in the handler body. Prevents non-operator access to fleet-wide instance telemetry and mass remediation. T-DRIFT-CROSS-TENANT-001 is covered because the auth gate is the first line of defense against unauthorized drift reads. | `bash gov-infra/verifiers/sec/operator-drift-auth-gate.sh` (SEC-12) | `gov-infra/evidence/SEC-12-output.log` |
| Maintainability | T-WIRE-FLOOD-001 | C-WIRE-IDEMP | Wire-all remediation is idempotent and auditable | Source-level assertion that the `handleOperatorRemediateMCPDrift` handler enforces idempotency: active job scan via GSI2 UPDATE_ACTIVE, MCPOnly filtering on active jobs, duplicate-skip before UpdateJob creation, MCPOnly:true on created jobs, and audit emission with slug + job ID context. | `bash gov-infra/verifiers/mai/wire-all-idempotency.sh` (MAI-5) | `gov-infra/evidence/MAI-5-output.log` |
| Security | T-HG-LEGACY-DECL-001 | C-HG-DECL-CONTRACT | Hosted-genesis declaration production is five-body-only | Source-level assertion that no deployed Host Go code defines or references the generic legacy selector/wrapper vocabulary (`LegacyDeclarationContract`/`DeclarationContractFromVersions`/the no-contract prompt wrapper `MintConversationSystemPrompt(`), that fresh hosted-genesis runtime paths (MicroVM workload + aiworker fallback) resolve the declaration contract through the fail-closed `RequireFiveBodyDeclarationContractFromEnv` selector and never emit the legacy `DeclarationCodeBoundaries` code, that the fail-closed version parser `ParseFiveBodyDeclarationContract` exists, that builders guard on `IsFiveBody` and surface a missing/unknown contract as `operator_action_required` via `ErrDeclarationContractUnconfigured`, and that CDK still provisions the five-body contract env for the MicroVM image and ai-worker Lambda. | `bash gov-infra/verifiers/sec/hosted-genesis-declaration-contract.sh` (SEC-15) | `gov-infra/evidence/SEC-15-output.log` |
| Security | T-MICROVM-REG-001 | C-MICROVM-REGISTRY | Hosted-genesis MicroVM registry cache stays Host-owned | Source-level assertion that deployed Host code does not persist AppTheory's generic `runtimemicrovm.SessionRegistryRecord`, call `NewTableTheorySessionRegistry`, import TableTheory directly in the controller, or use an in-memory deployed registry fallback. Requires Host-owned `HostedGenesisMicroVMExecution` model/repository/adapter, SessionRegistry + SessionRegistryLister compile assertions, controller reconstruction from `HostedGenesisSession`, and state-table read/write permission. | `bash gov-infra/verifiers/sec/hosted-genesis-microvm-registry-boundary.sh` (SEC-14) | `gov-infra/evidence/SEC-14-output.log` |
| Security | T-HG-PROVIDER-STALL-001 | C-HG-PROVIDER-TELEMETRY | Hosted-genesis provider calls remain runnable, observable without content, and deterministically converge | Source-level assertion that OpenAI/Anthropic assistant streaming and declaration extraction emit per-SDK-event content-free telemetry plus a ten-second heartbeat; provider HTTP/whole-call/workload/MicroVM deadlines form one validated bounded envelope; endpoint-dispatched asynchronous work omits AppTheory `ProviderIdlePolicy` because AWS measures idle from inbound endpoint traffic; AppTheory propagates the Host-owned execution role to the documented default runtime log group; `provider_timeout` recovery prepares a current-image/current-role runtime through AppTheory Get/Terminate/Run/readiness before one Invoke and atomically binds its lifecycle identity to the TableTheory retry/debit transaction; authenticated wait-only reads never resume or nudge; and suspended/terminal/expired state converges through one exact status/version/turn-guarded TableTheory transaction while preserving exhausted retry guidance. Focused tests cover redaction, deterministic hashes/counts, beyond-idle-boundary execution, runtime log boundaries, hung-call failure, envelope ordering, fresh-runtime sequencing, no pre-dispatch retry/debit loss, declarations, suspension, lifecycle death, and restart guidance. | `bash gov-infra/verifiers/sec/hosted-genesis-provider-telemetry.sh` (SEC-16) | `gov-infra/evidence/SEC-16-output.log` |

> Add rows as needed for additional anti-drift (multi-module health, CI rubric enforcement, encryption/redaction semantics)
> and for repo-specific P0 gates.

## Framework Mapping (Optional)
No compliance framework is assumed for this repository (domain is `custom`). If a framework becomes in-scope later, store only requirement IDs/titles here; keep licensed standards text out of-repo.

## Notes
- Prefer deterministic verifiers (tests, static analysis, IaC assertions) over manual checklists.
- Treat this matrix as “source material”: the rubric/roadmap/evidence plan must stay consistent with Control IDs here.
